apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "2"
  creationTimestamp: null
  generation: 1
  labels:
    chart: spark-0.2.1
    component: spark-cluster-1-zeppelin
    heritage: Tiller
    release: spark-cluster-1
  name: spark-cluster-1-zeppelin
  selfLink: /apis/extensions/v1beta1/namespaces/data-analytics/deployments/spark-cluster-1-zeppelin
spec:
  progressDeadlineSeconds: 2147483647
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      component: spark-cluster-1-zeppelin
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: RollingUpdate
  template:
    metadata:
      creationTimestamp: null
      labels:
        chart: spark-0.2.1
        component: spark-cluster-1-zeppelin
        heritage: Tiller
        release: spark-cluster-1
    spec:
      containers:
      - env:
        - name: SPARK_MASTER
          value: spark://spark-cluster-1-master:7077
        image: panthergggg/zeppelin:v2 
        imagePullPolicy: IfNotPresent
        name: spark-cluster-1-zeppelin
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        resources:
          requests:
            cpu: 100m
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        volumeMounts:
        - mountPath: /data
          name: data-framework
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      volumes:
      - name: data-framework
        persistentVolumeClaim:
          claimName: data-framework
      securityContext: {}
      terminationGracePeriodSeconds: 30
status: {}
